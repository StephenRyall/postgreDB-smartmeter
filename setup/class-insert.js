const { insert } = require("./insert");






//Push a huge local csv to your postgresDB

//Initialise with connection string
// If table already set up, should be able to pull back all columns.

// const connectionTable = csvtoPostGres(connectionString, tableName)

// connectionTable.push(file, batc).then(() => )


// class connectionTable {
//     constructor(connectionString, tableName) {
//         this.db = pgp(connectionString);
//         this.tableName = tableName 
//     }

//     push(filePath) {

//     }
// }
// connectionTable.deleteAll();